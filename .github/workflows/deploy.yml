name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    name: Build and Deploy
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Install Rollup Linux binary
        run: npm install @rollup/rollup-linux-x64-gnu --no-save --legacy-peer-deps

      - name: Build project
        run: npm run build
        env:
          NODE_ENV: production
          VITE_SUPABASE_URL: https://owmgcguwmqdxerorgzje.supabase.co
          VITE_SUPABASE_ANON_KEY: sb_publishable_1gLOmZn2nkwpMnmVR36Q0g_1XxPBmH0
          VITE_CONTACT_API_URL: ${{ vars.VITE_CONTACT_API_URL || 'https://nexgenteck-contact-api.onrender.com/contact' }}

      - name: Add CNAME and 404.html
        run: |
          cp CNAME build/CNAME
          cp build/index.html build/404.html

      - name: Deploy to gh-pages branch
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build
          force_orphan: true
